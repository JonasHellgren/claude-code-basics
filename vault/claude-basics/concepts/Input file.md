

An **input file** contains data or instructions that an agent reads before performing a task. It can define values, constraints, criteria, or starting conditions, such as coordinates in `coordinates.md` or book requirements in `criterias.md`. Input files separate the task data from the agent instructions in `CLAUDE.md`. This makes the workflow easier to update, reuse, and verify without changing the agent itself.

[[Concepts]]

