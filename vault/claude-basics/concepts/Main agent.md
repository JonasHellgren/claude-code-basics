
The **main agent** is defined through the instructions in `CLAUDE.md`. It receives the user’s prompt, follows the project workflow, decides which steps to perform, delegates tasks to subagents, manages shared files and state, and produces the final result.

[[Concepts]]

