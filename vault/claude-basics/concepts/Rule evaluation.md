
Rule evaluation means that an agent makes decisions by applying predefined conditions to input data. The rules may define thresholds, categories, priorities, or logical relationships. The agent reads the available values, checks each relevant condition, and selects the result that matches the rules. This approach is transparent because the reason for each decision can be traced to explicit instructions rather than learned behavior.

[[Concepts]]
