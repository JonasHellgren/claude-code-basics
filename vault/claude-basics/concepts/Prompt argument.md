

A prompt argument is a value supplied by the user that changes how an agent performs a task. It can select a workflow, specify an operation, or control how the result is produced. Different arguments allow the same main agent to handle several related actions without requiring separate projects. This creates a simple command-based interface between the user and the agent.

[[Concepts]]
