
A **subagent** is a specialized agent that performs a specific part of a larger task. It is usually called by the main agent and follows its own role and instructions. For example, one subagent may generate a number, while another guesses it. Subagents make workflows easier to divide, reuse, and understand. Their results are returned to the main agent, which coordinates the overall process and produces the final output.

[[Concepts]]
