The **Folder Creator** example demonstrates how an AI agent can automatically create a predefined directory structure from a simple specification.

Instead of manually creating folders, the user provides a structure (for example in `structure.md` or `structure.txt`), and the agent reads it, interprets the hierarchy, and creates all directories in the correct locations.